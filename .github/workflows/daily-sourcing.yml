name: AI Sourcing Agent - Self-Learning Mode

on:
  schedule:
    # Run ONCE daily at 9 AM UTC (1 hour runtime)
    - cron: '0 9 * * *'   # 9 AM UTC only - FIXED: was running 4x wasting time
  workflow_dispatch:  # Manual trigger for testing

jobs:
  smart-vendor-discovery:
    runs-on: ubuntu-latest
    timeout-minutes: 70  # 1 hour runtime + 10 min buffer
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install Ollama
      run: |
        echo "Installing Ollama..."
        curl -fsSL https://ollama.com/install.sh | sh
        nohup ollama serve > ollama.log 2>&1 &
        sleep 10
        echo "Downloading model (qwen2.5-coder:3b)..."
        ollama pull qwen2.5-coder:3b
        echo "Ollama ready!"
    
    - name: Install Python dependencies
      run: |
        pip install --upgrade pip
        pip install langchain langchain-ollama langgraph requests python-dotenv
        # Install Playwright for web scraping
        pip install playwright
        playwright install chromium --with-deps
    
    - name: Migrate database schema
      run: |
        echo "Running database migration..."
        python migrate_database.py
    
    - name: Run AI Sourcing Agent (1 hour runtime)
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        USER_EMAIL: ${{ secrets.USER_EMAIL }}
        EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
      run: |
        echo "Starting AI Sourcing Agent V2 (1 hour runtime)..."
        python main_v2.py production
    
    - name: Commit results to repository
      run: |
        git config user.name "AI Sourcing Agent"
        git config user.email "agent@frameai.com"
        git add data/ || true
        git commit -m "🤖 Daily run: $(date +'%Y-%m-%d %H:%M UTC')" || echo "No changes to commit"
        git push || true
    
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: sourcing-reports-${{ github.run_number }}
        path: |
          data/reports/
          data/vendors.db
